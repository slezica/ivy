# Bug Hunt Report — 2026-07-10

**Branch under review:** `remove-ytdlp` (working tree, including uncommitted files)
**Method:** 8 parallel subsystem reviews (recent diff, actions/store, playback/sessions, clips/transcription, sync, database/storage, Android native, UI) followed by direct source verification of every High finding by the coordinating session. Harness: `npm test`, `tsc --noEmit`, `expo lint`, full Android `assembleDebug` build in the sandbox, APK/DEX inspection, and byte-level verification of the `ffmpeg-0.18.1` AAR from Maven Central.

**Verdict on the recent changes (yt-dlp removal): clean.** No regressions found in the branch diff itself. The bugs below are pre-existing. Details in §5.

> **Fix status (2026-07-11):** three waves of fixes. (1) Low-hanging findings, 24 commits (`af67386..3e156bd`): H1, H3, H6–H9, M3, M4, M12, M15, M19, M20, L4–L6, L10, L14–L16 + docs drift. (2) The **sync redesign** ([2026-07-11-sync-redesign.md](2026-07-11-sync-redesign.md)), 32 commits (`3a1847e..c85af5f`): H2, H4, H5 (dissolved by design), M5, M7, M8, M9, L7, L8, L9 + real-sqlite/fake-Drive test harness. (3) Clear-cause batch, 15 commits (`cc8befe..d143a66`): **M1, M2 (window shrunk to a microtask gap — see note), M10 (60-min grace period), M11, M16, M17, M18, L1 (cross-book part; trailing-edge pending decision), L2, L3, L11, L13**; **M13 dissolved** (the failing `queueChange` branch was removed by the redesign's Phase 1). Suite: 351 tests green, `tsc` clean, Kotlin compiles. Fixed findings are removed below; IDs keep their gaps. **Still open:** M6 (postponed — beta), M14 + L1-trailing (pending product decision), M2 residual microtask window, M10's broader TOCTOU family (grace period mitigates), L12 resolved (hooks.ts deleted).

Severity legend: **Critical/High** = data loss, permanent stuck states, or wrong behavior in a mainline flow. **Medium** = real defect, needs specific-but-plausible conditions. **Low** = edge case, hygiene, or minor UX defect.

Each finding is tagged **[verified]** (re-confirmed in source by the coordinator, or demonstrated by a repro) or **[reported]** (single reviewer's evidence, spot-checked plausible but not independently re-traced).

---

## 1. High

*(none open — all High findings fixed or dissolved; see Fix status above)*

## 2. Medium

### M6. Migrations are non-atomic, and migration 0 records completion *before* creating the schema [reported — POSTPONED]
`src/services/storage/database.ts` (`runMigrations`)

Migration 0's first statements create `status` and insert `migration = 0` before `files`/`clips`/`sessions` exist; nothing is transactional. App killed mid-migration → next launch either skips schema creation or throws "duplicate column" forever. **Postponed 2026-07-11**: single beta user, DB nuke acceptable. Revisit before multi-user release.

### M14. Seeking while paused is never persisted [reported — awaiting product decision]
`src/store/index.ts` (only `updateBookPosition` call site), `src/actions/seek.ts`

Position persists only from playback status/progress events; `TrackPlayer.seekTo()` while paused emits neither. Pause → scrub → app killed → resume at the stale position (which also syncs). Options: persist on every seek, or persist-on-app-background hook. See §7.

## 3. Low

- **L1 (residual). Position sync has no trailing edge** — the cross-book suppression was fixed (`throttleSameArgs`, commit `6ba71aa`), but the final position before a pause is still never queued (pause doesn't queue, the 30s window has no trailing call). Other devices miss the last ≤30s until a future mutation. Awaiting the same product decision as M14. *[verified]*

## 4. Documentation drift

Fixed on 2026-07-11 for all tracked docs. One leftover (untracked files): `docs/IMPROVEMENTS_1.md:29` / `docs/IMPROVEMENTS_2.md:28` recommend refactors involving the deleted `src/actions/load_from_url.ts` — stale guidance for future sessions. Also noted by the docs agent, out of its scope: `docs/BOOKS.md:77` ("a timestamp suffix prevents collisions") — files are actually named by book UUID.

---

## 5. The yt-dlp removal branch itself: verified clean

Every hunted class of removal residue came up empty:

- **No dangling references** to the downloader/`load_from_url`/yt-dlp anywhere in `src/`, `app/`, `android/`, `maestro/` (only an explanatory test comment). `tsc` clean, tests green.
- **The `YoutubeDLException` shim is exactly right.** Two reviewers independently downloaded the published `ffmpeg-0.18.1.aar` from Maven Central and disassembled `FFmpeg.class`: the only references into `com.yausername.youtubedl_android` are the class `YoutubeDLException` and one methodref, `<init>(String, Throwable)` — which the shim provides (public, correct package). The only other external classes `FFmpeg` touches (`SharedPrefsHelper`, `ZipUtils`, commons-io `FileUtils`) are all satisfied by the POM's runtime deps (`common-0.18.1`, `commons-io`). `:library` is not in the POM, so the shim is both necessary and sufficient. Bonus: `FFmpeg.init()` is `synchronized` upstream, so concurrent init from AudioSlicer + ChapterReader threads is safe.
- **`cancel_load_file`** correctly resets all four library fields and cancels only the copier; the opId-guarded updater prevents the background error path from clobbering the reset.
- **UI**: LibraryScreen menu/empty states and SettingsScreen are coherent post-removal; LibraryLoadingDialog degrades exactly as the plan predicted.
- **Packaging**: `expo.useLegacyPackaging=true` retained; `MainApplication.kt` registers the four remaining packages and not `FileDownloaderPackage`.
- **APK inspection** (see §6): `libffmpeg.so` present, shim class present in DEX, zero yt-dlp engine classes (`YoutubeDLRequest`, Python payloads) in DEX.

One nit: `AudioSlicerModule.kt`/`ChapterReaderModule.kt` still put the removed `packages/python|aria2c/usr/lib` dirs on `LD_LIBRARY_PATH` — harmless (missing entries are ignored), cosmetic cleanup only.

## 6. Build & runtime validation performed

- `npm test`: all suites green (18 suites / 206 tests at report time; 19 / 225 after the 2026-07-11 fixes).
- `npx tsc --noEmit`: clean after the fixes (7 test-file errors at report time).
- `expo lint`: warnings only (import order, `Array<T>` style).
- **Android build: BUILD SUCCESSFUL.** OpenJDK 17 + Android SDK (platform 36, build-tools 36, NDK 27.1) installed in the sandbox; `./gradlew :app:assembleDebug -PreactNativeArchitectures=arm64-v8a` compiled the branch from scratch, including the Kotlin native modules and the `YoutubeDLException` shim; `:app:compileDebugKotlin` re-verified green after the 2026-07-11 Kotlin fixes. (Sandbox notes for reproducibility: the NDK/toolchain binaries are x86_64-only and needed amd64 glibc installed for Rosetta; system arm64 `cmake`/`ninja` via `cmake.dir`; the stale `android/build/generated/autolinking/autolinking.json` contained Mac-absolute paths from a previous host build and had to be deleted; container memory had to be raised from 8GB — three Gradle daemons died to OOM compiling emulated C++ before that.)
- **APK/DEX inspection** (both the prior host-built APK and the fresh sandbox-built one): `libffmpeg.so` packaged, `YoutubeDLException` shim + `com.yausername.ffmpeg.FFmpeg` present in DEX, zero yt-dlp engine classes.
- **AAR verification**: `ffmpeg-0.18.1.aar` pulled from Maven Central; `javap -v` on `FFmpeg.class` to enumerate cross-module references.
- **Not possible in this sandbox**: on-device/emulator smoke test — no `/dev/kvm` (as the removal plan predicted). The FFmpeg *runtime* path (clip slicing, chapter read on a real device) therefore remains the one thing only a device test can confirm.

## 7. Open questions / qualifications

1. **L1 (position sync throttle)** may be a deliberate staleness tradeoff for single-book use; the cross-book suppression, however, looks unintended. Worth a decision rather than a silent fix.
2. **M14 (seek-while-paused not persisted)**: persisting on every seek would be trivial, but if the current behavior is intentional (avoid churning the outbox while scrubbing), a persist-on-app-background hook would cover the data-loss window instead.
3. The remaining sync findings (H2, H4, H5, M5, M7–M9) share one root: the push/pull phases assume more atomicity than Drive and the outbox provide. A design pass (deletion tombstones, manifest invalidation on 404, given-up-item surfacing, bootstrap gating) would address them as a family rather than as six patches.
4. The sync unit tests mock the DB entirely, which is how the outbox UNIQUE-key bug (fixed H1) and M7's visibility filter stayed invisible. A real-sqlite (or in-memory fake with real constraints) test harness for the outbox is recommended as part of any sync work.
5. Findings tagged *[reported]* were produced by a subsystem reviewer with file:line evidence and spot-checked for plausibility, but not line-by-line re-verified the way the High findings were. Treat the cited line numbers as accurate as of this commit.
