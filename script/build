#!/usr/bin/env bash
# Build the static website: copy web/ into dist/, served by the host.
set -euo pipefail

cd "$(dirname "$0")/.."

rm -rf dist
mkdir -p dist
cp -r web/. dist/

echo "build: web/ -> dist/"
